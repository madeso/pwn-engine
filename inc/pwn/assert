#ifndef PWN_ASSERT
#define PWN_ASSERT

#define Assert(x) do { if( (x)==0 ) _asm { int 3 }; } while( ::pwn::assert::kFalse() )

namespace pwn
{
	namespace assert
	{
		bool kFalse();
	}
}

#endif