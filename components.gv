digraph pwn_component_dependencies
{
	/* convert */
	core -> convert;
	math -> convert;

	/* core */

	/* engine */
	core -> engine;
	mesh -> engine;
	render -> engine;
	math -> engine;

	/* game */
	engine -> game;
	math -> game;
	render -> game;

	/* math */

	/* mesh */
	math -> mesh;

	/* render */
	mesh -> render;
	math -> render;
}